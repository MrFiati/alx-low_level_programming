#include <main.h>
/**
 * print_alphabet - prints a string of alphabet
 *
 * Return: string of letters a to z
 */
void print_alphabet(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	_putchar(c);
	_putchar('\n');
}
